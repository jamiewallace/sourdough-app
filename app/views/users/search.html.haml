%h2 #{@type.titleize} Results
- @users.each do |user|
  =link_to user.username, user
  %br
  = user.town_city
  %br
  = user.country
  %br
  = image_tag user.avatar
  %br
  = user.description
  %hr

#map-canvas-global

:javascript
  window.user_address = "#{@address}"
  window.users = #{@users.to_json}